def area(b, c):
    a = b * c
    print(f'A área do retângulo é de {a} u.a.')


h = float(input('Digite a largura do retângulo: '))
comp = float(input('Digite o comprimento do retângulo: '))

area(h, comp)
